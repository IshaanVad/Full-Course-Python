import math # have to import "math"

pi = 3.14
x = 1
y = 2
z = 3

print(round(pi)) # rounds number
print(math.ceil(pi)) # ceiling of number
print(math.floor(pi)) # floor of number
print(abs(pi)) # absolute value
print(pow(pi, 2)) # power function
print(math.sqrt(49)) # square root of number
print(max(x, y, z)) # max number of variables
print(min(x, y, z)) # min number of variables